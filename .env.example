# Copy to .env only when ready to connect a model. Never commit .env.
# No X credentials or publishing implementation exist in this preview.
TIWEN_LIVE_ENABLED=false
TIWEN_API_BASE_URL=
TIWEN_API_KEY=
TIWEN_MODEL=
# Per-process request ceiling, NOT a monetary budget. Also set provider spending limits.
TIWEN_MAX_CALLS=10
